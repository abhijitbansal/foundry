# Session: The Harness — v2 delta update

**Session:** 7
**Started:** 2026-07-18
**Date:** 2026-07-18
**Branch:** feat/harness-page-v2
**Last updated:** 2026-07-18

## Checkpoints

### Phase 1 — Research + plan

**Achieved:**
- Deep-scouted `~/projects/cubby/docs/harness/{ARCHITECTURE.md,EVALUATION.md,harness-explainer.html}` + `AGENTS.md` against the live foundry `/harness/` page (two parallel Explore agents), establishing the score-stratification timeline: 71→73 (session 0025) → 79 (session 0028, `harness-to-90`, what the foundry page currently shows) → an unscored s56/s57 wave (2026-07-18) that the source docs' own EVALUATION.md addendum explicitly declines to re-score ("No new score is claimed").
- Drafted `docs/plans/2026-07-18-harness-page-v2.md` via a planner-tier (Opus) agent, then independently fact-checked the central claims against primary sources myself (not the scout summaries) — caught and fixed two real errors before build: a wrong incident count (the draft said "9+" session-number collisions; AGENTS.md's own text says "≥8") and a missing delta item (the tracker's archive pipeline — 63 items archived, ledger down to 649 lines — was in EVALUATION.md's addendum but absent from the draft's 10 items). Both fixed, item count became 11.

### Phase 2 — Build

**Achieved:**
- `src/data/harness.ts` + `harness.types.ts` — typed `SCALARS` + 11 `DELTA_ITEMS`, meant as the single edit-point for the next re-score.
- New section "07 · The retool" (`HarnessDelta.astro`), an 11-item badged card grid (NEW/RULE/APPLIED/RETIRED/CLOSED/CHANGED/NOTE), placed between the audit (06) and the footer CTA. Carries **no new score number** — mirrors the source docs' own "status deltas only" stance.
- Factual refresh across Paper (AGENTS.md size), Arsenal (plugin count, several relative-time phrases), Score (a "what changed since" anchor), and figure-adjacent text callouts on Lifecycle and Tracker (zero SVG bytes touched — both figures are locked/collision-tuned).
- Page-wide staleness self-audit grep caught three more relative-time phrases the plan hadn't explicitly named (Tracker's "shipped this week" kicker, Arsenal's "declared this month", FooterCTA's "drawn from the live config" — the last one a real accuracy bug the original cubby research had already flagged as false, since the numbers are hardcoded, never live).
- `tests/unit/harness-data.test.ts` (4 tests) + full `npm run build && npm test` green (97 tests total). Chrome DevTools MCP visual QA: section 07 and every touched section in light + dark + 390px mobile, console clean, fullscreen figure count unchanged at 5 (section 07 correctly has no fullscreen tab).

### Phase 3 — Code review (Workflow, xhigh) + fixes

**Achieved:**
- Workflow-backed review: 6 finders, 8 verifier agents, 9/9 candidate findings confirmed, 5 reported after consolidation. All fixed:
  - **Correctness**: the ownership callout conflated the retired s45 metrics-capture system with the still-active (paused) instinct observer actually drawn in the Loop figure — different systems, wrong claim about what the frozen figure shows. Corrected.
  - **Accessibility**: `.ds-chip--warn` text failed WCAG AA in light mode (~4.06–4.36:1 against its own tinted background, need 4.5:1) — added a `[data-theme="light"]`-scoped darker override (#6B5210, ~5.7:1), the exact pattern `global.css` already uses for the accent/hover link-color swap. Dark mode (~7.4–8.2:1) untouched.
  - **Content-review gap**: section 07's script/doc filenames and session-wave numbers sit outside the *literal* text of AGENTS.md's Paper-section private-repo exception (2026-07-14). Extended that row to name section 07 explicitly, with the checkpoint recorded inline — closes the missing paper-trail the finding flagged, doesn't change what's published (already non-secret, non-URL, same category as content the Score section already cites).
  - **Test-fixture drift**: `VALID_BADGES` in the test was a hand-copied literal array instead of derived from `DeltaBadge` — refactored `harness.types.ts` to export `DELTA_BADGES` as a `const` tuple with `DeltaBadge` derived via `typeof`, so the test and the type now share one source.
  - **DRY/staleness cluster**: `SCALARS` was built as "the single edit-point for a re-score" but never actually wired into its consumers — `HarnessArsenal.astro`, `HarnessPaper.astro`, and one `DELTA_ITEMS` body all independently hardcoded numbers that already lived in `SCALARS`. Wired all three through it; verified in-browser post-fix that Arsenal's card copy now genuinely reads from the shared module.
- Rebuilt + retested green (97/97) after fixes; re-screenshotted the two visually-affected spots (warn-chip contrast, Arsenal card copy) to confirm in-browser, not just by re-reading the CSS.

**Decisions:**
- v2 is a scoped label (chips + one new section), not a page relaunch — the s56/s57 wave is hardening/one retirement/one closed investigation, not a new architectural layer. Re-titling the whole page would over-claim a rebuild that didn't happen.
- Section 07 carries no new score number, by design — the source docs themselves decline to re-score as of 2026-07-18, and inventing one would violate the exact "independent re-score, never self-asserted" principle the page's own audit section is built around.
- `astro check` was skipped (not installed in this repo; would require adding `@astrojs/check` + `typescript` as new devDependencies unprompted) — `astro build`'s own type-aware compilation pass already covers `.astro`/`.ts` correctness for this diff.
- The full automated data-refresh pipeline (script-generated `src/data/harness.ts`, mirroring `data/stats.json`'s pattern) stays explicitly deferred — cubby is a separate private repo, absent at foundry build time, so any script would still be a manual copy-paste. The typed `SCALARS` module is the in-scope half of that idea.

**Follow-ups:**
- The Arsenal plugin grid still shows only the 13 originally-named plugins under "a sample of the enabled set" (count says 17) — the 4 new plugin names weren't in the research and weren't fabricated. Getting the real 4 names from cubby (with the operator's help) and adding them as chips is a nice-to-have, not blocking.
- If cubby's harness docs ever expose a machine-readable stats file the operator is willing to vendor, revisit the deferred automated-refresh pipeline.
- Manual-test checklist (`.scratch/feat-harness-page-v2-test-checklist.html`) covers what this session's automation couldn't reach — real device badge contrast, screen reader pass on section 07, real-phone anchor tap — still open.
- **User decision, end of session:** branch stays open, deliberately unmerged — more changes land on it next week before publish. Not a stalled branch; don't merge or close PR #22 without being asked. The LinkedIn post for this work will use a different angle than the default announcement template (session 6's 2026-07-15 harness-launch post) — check with the user for the angle once the branch is actually ready to publish.

**Resume pointer:** PR #22 (`feat/harness-page-v2` → `main`) is open at https://github.com/abhijitbansal/foundry/pull/22, 3 commits (plan, build, review-fix), build/test/review all green. Held open per user request — next session on this branch should pull latest `main` first (check for drift), read this log plus PR #22's current state, then add whatever the user brings before re-running the ship gates (build/test/review) and merging.

**Models:** Sonnet 5 (executor, this session) for planning synthesis, implementation, and review-fix application; one Opus dispatch for the initial plan draft (planner tier); workflow-backed code review ran its own 17-agent fleet at xhigh effort.
