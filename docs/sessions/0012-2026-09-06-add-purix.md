# Session 0012 — 2026-09-06 — Add Purix to the site and the weekly digest

**Branch:** `feat/add-purix` · **Session:** `session_013TYjVt3i4Vx2X4hsxvzgC2`

## Achieved

- **New project card.** `Purix` added to `appsProjects` in `src/data/projects.ts` — a browser extension that strips tracking parameters from links on-device. `github.com/abhijitbansal/purix` is private, so the card is curated blurb + status only, no repo or site link (`private: true`, which `tests/unit/projects.test.ts` enforces). The blurb sticks to the product claims a public README already states: blocklist-only, hard keep-list, zero network calls, Chrome/Firefox/Safari.
- **Apps group is now five.** `WorkSection.astro`: label `Apps — iOS, macOS & browser` (Chrome and Firefox are neither), count `05`, following groups' `startIndex` 5→6 and 9→10, lead copy "Nine projects"→"Ten projects". Test count 9→10 (written first, watched it fail).
- **Weekly digest.** `"purix"` added to `PROJECTS_ALLOWLIST` in `scripts/stats/parse_sessions.py`. That is the entire change needed for `/updates`: the weekly pipeline derives its repo list from session transcripts, and `repo_from_path` buckets both `~/projects/purix` and its `.claude-worktrees/*` sessions to `purix`. The first digest to carry it is 2026-W36, generated by the Monday 2026-09-07 run.
- **Yard slot, same commit as the allowlist.** `works.ts:assertYardCoverage` throws when `data/stats.json` gains a repo with no `YARD` entry, so shipping the allowlist without the slot would have failed CI on that Monday digest PR. Purix sits on plate 0 at `x: 4.9, y: 1.3` (`monitor` archetype), plus a `STRIP_ARCHETYPES` entry so the weekly strip building keeps a stable silhouette.
- **Regenerated `data/stats.json` / `stats-archive.json`** so Purix is in the yard today rather than after the next automation run. It enters at rank 02 — 107 sessions, 33,661 lines added, 6 storeys.
- Gates: `npm run build` and `npm test` (109 tests) green; reviewer pass on the diff.

## Decisions

- **Purix lives in the Apps group, not a new one.** It is a shipped end-user product, and it does ship Safari targets for macOS and iOS; the group label absorbs "browser" rather than the site growing a fourth group for one card.
- **Nameplate moved off the building's front corner** to `[9.0, 2.6]`. The conventional front-corner position `[7.4, 3.3]` was measured in-browser as painted over by Floorprint's roof (drawn later in depth order) — `document.elementFromPoint` around the badge circle returned a `polygon`, not the badge. Candidate positions were tested the same way before picking one.
- **`.fy-cards--lead4`** added to `global.css` + `WorkGroup.astro`. The existing lead grid spans followers across 2 of 6 columns, which fits 1+3 exactly; 1+4 stranded the fourth card at a third of the row. The modifier spans followers across 3 columns for a 2x2, and applies only when a lead group has exactly five projects.

## Follow-ups

- Purix's store listings are "coming soon" in its README. When Chrome/Firefox/Safari listings go live, the card should gain a `siteUrl` — and if the repo ever goes public, a `repoUrl`.
- **`claude-skills` left the yard in this session's regeneration.** `main`'s `data/stats.json` (generated Aug 31, `files_found: 1043`) listed it; this run saw 906 transcript files and dropped it, because `parse_sessions.py` only counts sessions whose transcripts are still on disk. Monday's automation would have done exactly the same, so the commit is right — but a building disappearing from the homepage yard is a user-visible change, and the yard now reads as an all-time plan while being a "transcripts still on disk" plan. Worth deciding whether the yard should merge `stats-archive.json` so a repo's building persists once earned.

## Resume pointer

Merge the PR before Monday 09:00 EDT. Not because a half-state can break the build — the allowlist and the YARD slot ship in one commit, so `main` can never hold one without the other. The reason is conflicts: the Monday run regenerates `data/stats.json` and `data/stats-archive.json` on its own branch, and if this PR is still open, whichever of the two merges second goes `mergeable: dirty` on those exact files — the same wedge session 0011 spent an afternoon unpicking.

## Models

- Solo, Opus 5 (this session) for the edits and in-browser verification; one `ecc:code-reviewer` dispatch (Opus 5) for the diff review gate.
