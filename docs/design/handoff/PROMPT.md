# Kickoff prompt for Claude Code

Copy-paste the block below into Claude Code from the `foundry` repo root (put this handoff folder at `docs/design/handoff/` first).

---

Implement the Foundry portfolio site from the design handoff in `docs/design/handoff/` — read its `README.md` fully before writing any code; it is the spec.

Summary of the job:

1. **Scaffold**: Astro + vanilla Three.js (rationale in the README), static output, GitHub Pages deploy workflow, custom domain `abhijitbansal.com` (CNAME), hash anchors only.
2. **Recreate variant 1A ("Crucible")** pixel-perfectly from `Foundry-1A-Crucible.html` (open it in a browser) and its readable source in `src/`. All colors/type/spacing come from `tokens/*.css` — consume tokens from my `design-system` repo if practical, otherwise vendor them.
3. **3D hero**: port the `_buildScene` code from the source file into a lazy-loaded module (dynamic import on idle/hero-intersection, self-hosted three, DPR ≤ 2, IO pause, reduced-motion/no-WebGL → the CSS poster fallback that's already in the design). Keep the scroll-cooling behavior.
4. **Projects data + links**: build `src/data/projects.ts` per the README's link-resolution rules — companion `*-site` public repos (cubby-site → gotcubby.com, paperix-site, floorprint-site) get site links; repos with in-repo Pages sites (sift, possibly cartoon/claude-skills) get repo + site links; private repos (folix, memekit, design-system, the app repos) get status + blurb only, no links. Resolve actual Pages URLs from each repo's CNAME/Pages config and verify them.
5. **Telemetry section**: numbers bake at build from `data/stats.json` (generated by my claude-skills stats script). Add a small build step that reads the JSON and emits the section's stats, the 30-day heatmap, and the `LAST FORGED · <build date>` pill. Regenerate the memekit terminal block via memekit if easy, else keep the static reaction.
6. **Meta**: title/description/canonical, OG + Twitter card with a generated 1200×630 image, favicon set from the ember-tab mark, sitemap, robots. Zero analytics/trackers.
7. Run through the README's acceptance checklist (perf budgets, a11y, link 200s) before calling it done.

Work design-first: get the static DOM pixel-perfect at 1440/1024/390 before touching Three.js.
